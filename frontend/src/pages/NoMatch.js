import React from "react";
import { Layout } from "../components/Layout";

export const NoMatch = () => (
  <Layout>
    <h2 className="text-center">No se encontro lo que estabas buscando</h2>
  </Layout>
);
